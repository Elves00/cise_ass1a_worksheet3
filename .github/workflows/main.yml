name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.8dddbdf0359641679d029e75056aed4b}}
          heroku_app_name: "cise_ass1a_worksheet" #Must be unique in Heroku
          heroku_email: "breconrose@gmail.com"
